{
  "id": "c0c0c0c0-0073-0000-0000-000000000000",
  "name": "Structured Notes",
  "type": "871ae771-b9b1-4f40-8c7f-d9038bfb69c3",
  "created": 1740700800000,
  "modified": 1772928000000,
  "content": {
    "description": "**Status:** Merged into notes\n\n## Summary\n\nStructured notes are now part of the regular note type. Any note can have `content.computed` fields and `{{field-name}}` references in its `content.description`. Uses [Reactive Computed Fields](item://c0c0c0c0-0074-0000-0000-000000000000) as the single underlying mechanism — a transclusion is just a computed field whose formula mirrors another item's field. Notes without computed fields render exactly as before.\n\n## The Unification\n\nThe original version of this proposal introduced four Hob page primitives (`text`, `transclude`, `val`, `link`) and a new page format where pages are Hob expressions. Through iteration, a simpler insight emerged: all of these collapse into one mechanism — computed fields with `get-field` as uniform access.\n\n| What you want | What it actually is |\n|---|---|\n| Transclusion of another note | `(get-field @other-note :description)` — a computed field mirroring another item's field |\n| Inline computed value | `(* (get-field :self :quantity) (get-field :self :price))` — a computed field with logic |\n| Functional transclusion (`?call=`) | `(invoke (require \\\"module\\\") \\\"fnName\\\" api params)` — a computed field calling a module export |\n| Aggregation | `(reduce + 0 (map (fn [i] (get-field i :amount)) (query-tagged @expense-tag)))` — a computed field over a query |\n\nOne mechanism. One reactive system. One way to track dependencies. No special transclusion primitives needed.\n\n## Note Structure\n\nAny note (type `871ae771-...`) can use computed fields:\n\n```json\n{\n  \"type\": \"871ae771-b9b1-4f40-8c7f-d9038bfb69c3\",\n  \"content\": {\n    \"description\": \"Here is a summary: {{summary}}. The total budget is **{{total-budget}}**.\",\n    \"quantity\": 10,\n    \"unit-price\": 25.50,\n    \"computed\": {\n      \"summary\": [\"get-field\", \"@other-note\", \":description\"],\n      \"total-budget\": [\"*\", [\"get-field\", \":self\", \":quantity\"],\n                            [\"get-field\", \":self\", \":unit-price\"]]\n    }\n  }\n}\n```\n\n- `content.description` — markdown string with `{{field-name}}` references. Uses the standard note field.\n- `content.computed` — map of field names to Hob formulas. Evaluated lazily via `get-field`. See [Reactive Computed Fields](item://c0c0c0c0-0074-0000-0000-000000000000).\n- Static fields (`quantity`, `unit-price`) coexist alongside computed fields normally.\n\n## Authoring Experience\n\nThe editor preserves the feel of writing a note:\n\n- **Prose**: type normally in CodeMirror — markdown, same as today.\n- **Field references**: insert `{{field-name}}` anywhere in the body to reference a named field (static or computed).\n- **Links**: use existing `item://id` syntax — unchanged.\n- **Field management**: a side panel or inspector for creating and editing computed fields — name, Hob formula, current value.\n\nOne syntax, one concept: `{{field-name}}` always means \"resolve this field via `get-field`.\" Whether the field is a transclusion, a calculation, or a static value is defined in the field itself, not in the body syntax.\n\n### Editor gestures\n\nInserting a transclusion is two logical steps — create the computed field, then reference it — but the editor streamlines this into a single gesture: an \"insert transclusion\" action prompts for a target item and a field name, then creates both the `content.computed` entry and the `{{name}}` reference at the cursor.\n\n### Storage\n\nWhat you see in the editor is what's stored. `{{field-name}}` in the body maps 1:1 to a key in `content.computed` (or a static field in `content`). No compilation step, no generated names, trivial round-tripping.\n\n## Rendering\n\nThe note readonly view (a generic-view delegate) renders via `field-view-markdown-readonly`, which:\n\n1. Detects `{{field-name}}` patterns in `content.description`.\n2. For each reference, calls `api.getField(itemId, fieldName)` — a JS API that delegates to the Hob `get-field` function.\n3. Wraps resolved values in `<span class=\\\"field-ref\\\">` with dashed underline styling and click-to-scroll behavior.\n4. Renders the result as markdown via `hobson-markdown`.\n\nA collapsible \\\"Fields\\\" section (rendered by `field-view-field-summary`) shows:\n- Static fields as `name: value`\n- Computed fields with formula (via `field-view-hob-structural`), resolved value, and provenance links to referenced items.\n\nFor notes without `{{` references or computed fields, neither the ref resolution nor the field summary activates — rendering is identical to before.\n\n## Implementation Progress\n\n### Completed\n\n- **Merged into note type**: Structured note capabilities are now part of the regular note type (`871ae771-...`). The note readonly and editable views handle computed fields, static fields, and `{{field-name}}` references.\n- **`get-field` function** in the Hob interpreter: unified field access with computed field support. Resolves item refs, keywords, and string field names. Checks `content.computed` for formulas, evaluates via Hob evaluator, falls back to item root then `content`. Includes cycle detection via `_computingFields` Set.\n- **`api.getField`** on the rendering API: JS-callable wrapper around Hob `get-field`, enabling field views and generic-view delegates to resolve computed fields without needing the Hob interpreter directly.\n- **`resolve-field-refs`** in Hob stdlib: resolves `{{field-name}}` references in a string using `get-field` — used for cross-item body transclusion.\n- **`field-view-markdown-readonly`**: Updated to detect `{{field-name}}` patterns, resolve them via `api.getField`, wrap resolved values in clickable `<span class=\\\"field-ref\\\">` with dashed underline styling and click-to-scroll behavior. No effect on notes without `{{` references.\n- **`field-view-field-summary`**: Readonly combined summary showing static fields and computed fields. Computed fields display formula (via `field-view-hob-structural`), resolved value, and provenance links to referenced items (extracted via JSON AST walk).\n- **`field-view-computed-fields`** (`e8d7c6b5-...-5c4e`): Editable field view rendering `content.computed` as name/formula pairs. Each formula edited via `field-view-hob-structural`. Supports add/remove/rename.\n- **`field-view-static-fields`** (`e8d7c6b5-...-5c4f`): Editable field view for arbitrary static content fields, with caller-specified `exclude` list.\n- **`generic-view`** updated: passes `item` to field views via options; skips field views that render as `display: none`.\n- **Provenance links**: Computed field formulas are walked (JSON AST) to extract `@`-prefixed item references. Each referenced item is displayed as a clickable link in the field summary, using `api.openItem` for navigation.\\n- **Functional transclusions**: Computed fields can call module exports that return DOM elements. `require` moved from view-only to stdlib (available in all Hob contexts). The rendering `api` is passed into the formula evaluation environment via `_evalApi`, so formulas can call `(invoke (require \\\"module\\\") \\\"fn\\\" api params)`. DOM results are mounted into placeholder spans after markdown rendering.\\n- **`require` in stdlib**: Moved from `registerViewOps` to `registerItemOps` — now available in computed field evaluation, REPL, event handlers, not just views. Uses `api.require` from the kernel module system.\n\n### Known Limitations\n\n- **Cross-item reactivity**: The view only re-renders when its own item changes. If a computed field references another item and that item changes, the view doesn't re-render. See [Reactive Computed Fields](item://c0c0c0c0-0074-0000-0000-000000000000) for the assessment (~85 lines to fix).\n- **No caching**: Computed fields are re-evaluated on every access. Fine for current use; caching is a future optimization.\n- **No error sentinels**: Formula errors are caught and shown as empty or `[error]`. The proposal describes error items and `ERR` links — not yet implemented (~40 lines). See [Reactive Computed Fields](item://c0c0c0c0-0074-0000-0000-000000000000).\n- **Existing structured notes need migration**: Items created with the old `structured-note` type and `content.body` need their type changed to the note type and `content.body` renamed to `content.description`.\n\n### Next Steps\n\n- Migrate existing structured-note items to the note type.\n- Author real notes to evaluate the experience.\n- Consider cross-item reactivity improvements if the experiment proves valuable.\n\n## Experiment Plan\n\n1. ~~Create the `structured-note` type definition.~~ Done.\n2. ~~Implement the [Reactive Computed Fields](item://c0c0c0c0-0074-0000-0000-000000000000) mechanism.~~ Done (basic `get-field` with cycle detection; caching deferred).\n3. ~~Build a basic view and editor for structured notes.~~ Done.\n4. ~~Add provenance links.~~ Done (AST inspection approach).\n5. ~~Merge into the note type.~~ Done — note views now handle computed fields, `{{refs}}`, and field summary.\n6. Author real notes to evaluate the experience.\n7. Evaluate: does this add meaningful value over plain markdown notes?\n\n## Provenance and Linking (Resolved)\n\nImplemented using **approach A: AST inspection**. The `field-view-field-summary` walks each computed field's JSON AST to find `@`-prefixed item references. Each referenced item is displayed as a clickable link below the formula, using `api.openItem` for navigation with modifier key support (Ctrl/Cmd = new tab, Alt = root, plain = attach).\n\nThis covers the common cases:\n- `(get-field @item :field)` — link to the referenced item\n- `(some-function @item)` — link to the target item\n- Complex formulas with multiple refs — all referenced items shown\n- Self-referential formulas — no external links\n\nFunction provenance (linking to the code item that defines a function) is not yet implemented — it would require `_symbols` metadata search, which can be added later if needed.\n\n## What This Enables\n\n- **Reactive transclusions** that update when the source changes — same `get-field` mechanism as any computed field.\n- **Live computed values** inline in prose — item counts, field references, aggregations, projections.\n- **Functional transclusions** — applying a function to an item and embedding the result, replacing the `?call=` URL parameter convention with a proper computed field.\n- **Queryable references** — computed field formulas are JSON AST, walkable and inspectable without parsing markdown.\n- **Spreadsheet-like calculations** across items — budgets, projections, derived metrics.\n- **One mechanism** for all reactive data — no separate systems for transclusions, computed values, and aggregations.\n\n## What This Does Not Do\n\n- **Break existing notes.** Notes without `{{` references or `content.computed` render exactly as before.\n- **Replace markdown for prose.** Bold, italic, headings, lists — still markdown, still rendered by markdown-it.\n- **Require learning Hob to write notes.** Writing Hob formulas is opt-in for computed values. Simple notes are still simple.\n- **Change the item model.** Items still have `content` maps. `content.computed` is a new convention, not a schema change.\n\n## References\n\n- [Reactive Computed Fields](item://c0c0c0c0-0074-0000-0000-000000000000) — The underlying mechanism: computed fields, `get-field`, caching, reactivity, cycle detection.\n- [Hob Language Design Direction](item://c0c0c0c0-0058-0000-0000-000000000000) — Hob as the primary user-programming language.\n- [Rich Transclusion Support](item://c0c0c0c0-0035-0000-0000-000000000000) — The existing transclusion system in markdown notes.",
    "tags": [
      "c0c0c0c0-0040-0000-0000-000000000000"
    ]
  },
  "attachments": []
}
