{
  "id": "d0d0d0d0-0011-0000-0000-000000000000",
  "name": "image-view",
  "type": "aaaaaaaa-0000-0000-0000-000000000000",
  "created": 1774607975531,
  "modified": 1775926313675,
  "content": {
    "for_type": "d0d0d0d0-0010-0000-0000-000000000000",
    "displayName": "Image",
    "description": [
      ":doc",
      [
        ":p",
        {
          "s": "Renders an image from content.data (base64 data URL)."
        }
      ]
    ],
    "code": "// image-view — Renders an image item\n// Fetches the blob from the blob store by item ID, converts to data URL for display.\n\nexport async function render(item, api) {\n  const el = api.createElement('div', { className: 'image-view' });\n  el.style.cssText = 'display: flex; flex-direction: column; align-items: center; gap: 8px; padding: 8px;';\n\n  try {\n    const blob = await api.getBlob(item.id);\n    const dataUrl = await new Promise((resolve, reject) => {\n      const reader = new FileReader();\n      reader.onload = () => resolve(reader.result);\n      reader.onerror = reject;\n      reader.readAsDataURL(blob);\n    });\n    const img = api.createElement('img');\n    img.src = dataUrl;\n    img.alt = item.content?.alt || item.name || '';\n    img.style.cssText = 'max-width: 100%; height: auto; border-radius: var(--border-radius);';\n    img.draggable = false;\n    el.appendChild(img);\n  } catch {\n    const placeholder = api.createElement('div');\n    placeholder.style.cssText = 'padding: 2em; color: var(--color-text-secondary); text-align: center; border: 2px dashed var(--color-border); border-radius: var(--border-radius);';\n    placeholder.textContent = 'No image data';\n    el.appendChild(placeholder);\n  }\n\n  return el;\n}\n",
    "_symbols": {
      "render": {
        "name": "render",
        "line": 4,
        "col": 7,
        "endLine": 30,
        "endCol": 1,
        "kind": "function",
        "scope": null,
        "signature": "(item, api)"
      },
      "render.el": {
        "name": "el",
        "line": 5,
        "col": 8,
        "endLine": 5,
        "endCol": 66,
        "kind": "variable",
        "scope": "render"
      },
      "render.blob": {
        "name": "blob",
        "line": 9,
        "col": 10,
        "endLine": 9,
        "endCol": 43,
        "kind": "variable",
        "scope": "render"
      },
      "render.dataUrl": {
        "name": "dataUrl",
        "line": 10,
        "col": 10,
        "endLine": 15,
        "endCol": 6,
        "kind": "variable",
        "scope": "render"
      },
      "render.img": {
        "name": "img",
        "line": 16,
        "col": 10,
        "endLine": 16,
        "endCol": 40,
        "kind": "variable",
        "scope": "render"
      },
      "render.placeholder": {
        "name": "placeholder",
        "line": 23,
        "col": 10,
        "endLine": 23,
        "endCol": 48,
        "kind": "variable",
        "scope": "render"
      }
    }
  },
  "attachments": []
}