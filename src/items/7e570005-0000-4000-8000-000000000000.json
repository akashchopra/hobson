{
  "id": "7e570005-0000-4000-8000-000000000000",
  "name": "note-render-tests",
  "type": "22222222-0000-0000-0000-000000000000",
  "created": 1739059200000,
  "modified": 1739059200000,
  "content": {
    "code": "const NOTE_TYPE = '871ae771-b9b1-4f40-8c7f-d9038bfb69c3';\n\nexport async function run(api) {\n  const { createSuite, assert, waitFor } = await api.require('test-lib');\n  const { testAsync, getResults } = createSuite('Note Rendering');\n\n  async function rendersWithoutCrashing() {\n    const id = crypto.randomUUID();\n    await api.set({\n      id,\n      type: NOTE_TYPE,\n      content: { description: 'Render test note' }\n    });\n    const el = await api.openViewport(id);\n    try {\n      assert(el, 'openViewport should return a DOM element');\n      assert(el.children.length > 0, 'Rendered note should have child elements');\n    } finally {\n      api.closeViewport(el);\n      await api.delete(id);\n    }\n  }\n\n  async function rendersTitle() {\n    const id = crypto.randomUUID();\n    await api.set({\n      id,\n      name: 'Test Title XYZ',\n      type: NOTE_TYPE,\n      content: { description: 'Body text here' }\n    });\n    const el = await api.openViewport(id);\n    try {\n      const found = await waitFor(() => {\n        return el.querySelector('[data-field=\"name\"], h1, h2');\n      });\n      assert(found, 'Should find a title element');\n      assert(\n        found.textContent.includes('Test Title XYZ'),\n        'Title element should contain the note name'\n      );\n    } finally {\n      api.closeViewport(el);\n      await api.delete(id);\n    }\n  }\n\n  async function rendersMarkdownBody() {\n    const id = crypto.randomUUID();\n    await api.set({\n      id,\n      type: NOTE_TYPE,\n      content: { description: 'This has **bold text** in it' }\n    });\n    const el = await api.openViewport(id);\n    try {\n      const found = await waitFor(() => {\n        return el.querySelector('strong, b');\n      });\n      assert(found, 'Markdown should render bold text as <strong> or <b>');\n      assert(\n        found.textContent.includes('bold text'),\n        'Bold element should contain the expected text'\n      );\n    } finally {\n      api.closeViewport(el);\n      await api.delete(id);\n    }\n  }\n\n  await testAsync('renders a note without crashing', rendersWithoutCrashing);\n  await testAsync('renders note title', rendersTitle);\n  await testAsync('renders markdown body', rendersMarkdownBody);\n\n  return getResults();\n}\n",
    "description": "Rendering tests for notes: validates that notes render without crashing, display their title, and render markdown body correctly.",
    "tags": [
      "c0c0c0c0-0070-0000-0000-000000000000"
    ]
  },
  "attachments": []
}
