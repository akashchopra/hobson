{
  "id": "c0c0c0c0-0016-0000-0000-000000000000",
  "name": "Common Code Gotchas",
  "type": "871ae771-b9b1-4f40-8c7f-d9038bfb69c3",
  "created": 1738454400000,
  "modified": 1771276113517,
  "content": {
    "description": "Documented pitfalls discovered during Hobson development. Each was a real bug that cost debugging time.\n\n## The parseInt Zero Trap\n\n**Problem:** `parseInt(\"0px\") || fallback` treats 0 as falsy.\n\n```javascript\n// WRONG — window at x=0 jumps to fallback position\nconst x = parseInt(wrapper.style.left) || 50;\n\n// CORRECT — 0 is a valid position\nconst leftPx = parseInt(wrapper.style.left);\nconst x = isNaN(leftPx) ? 50 : leftPx;\n```\n\n**Why this matters:** Window position (0, 0) is valid. The `||` operator treats `0` as falsy, triggering the fallback. The `isNaN()` check correctly detects only parse failures.\n\n**Pattern to use everywhere:**\n```javascript\nconst parsed = parseInt(string);\nconst value = isNaN(parsed) ? defaultValue : parsed;\n```\n\n## Backward Compatibility: String → Object Attachments\n\nAttachments evolved from string arrays to positioned objects:\n\n```javascript\n// Old format\nattachments: [\"item-1\", \"item-2\"]\n\n// Current format\nattachments: [{ id: \"item-1\", view: { type: \"...\", x: 20, y: 20, ... } }]\n```\n\nAll code that reads attachments must handle both:\n\n```javascript\nconst childId = typeof child === 'string' ? child : child.id;\n```\n\nString-format attachments convert to object format on first interaction.\n\n## Reading Position from DOM, Not Closures\n\nWhen starting a drag, read the current position from the DOM element — not from the render-time closure:\n\n```javascript\n// WRONG — uses stale render-time data\nconst startLeft = child.x;\n\n// CORRECT — reads actual current DOM position\nconst leftPx = parseInt(wrapper.style.left);\nconst startLeft = isNaN(leftPx) ? child.x : leftPx;\n```\n\nSee [Stale Closure Prevention](item://c0c0c0c0-0011-0000-0000-000000000000) for the full pattern.\n\n## Updating Multiple Properties Together\n\nWhen modifying item data, include all properties — not just the one you're changing:\n\n```javascript\n// WRONG — loses other properties when converting string to object\nconst updates = { x: newX, y: newY };\n\n// CORRECT — includes all properties for complete data\nconst updates = {\n  x: newX, y: newY,\n  width: width, height: height,\n  z: currentZ\n};\n```\n\nThis is especially important when converting string-format attachments to object format.\n\n## Global Listeners for Drag\n\nAttach `mousemove` and `mouseup` listeners to `document`, not to the element being dragged:\n\n```javascript\n// WRONG — loses drag if mouse leaves the element\ntitlebar.onmousemove = handleDrag;\n\n// CORRECT — captures drag everywhere\ndocument.addEventListener('mousemove', handleDrag);\ndocument.addEventListener('mouseup', handleDragEnd);\n```\n\nThe mouse can leave the window during a drag. Titlebar-only listeners lose the interaction.\n\n## setTimeout(0) for Post-Render Work\n\nAfter replacing DOM content, use `setTimeout(..., 0)` to defer operations that depend on the new DOM being in place:\n\n```javascript\nmainView.appendChild(newContent);\nsetTimeout(() => {\n  // Now the DOM is fully rendered and measurable\n  restoreScrollPositions();\n}, 0);\n```\n\n## Hob Atoms and Re-render Loops\n\n**Problem:** A Hob view creates atoms during its tracked evaluation. When a click handler calls `(reset! my-atom new-val)`, the atom mutation callback triggers a full re-render of the same view, creating fresh atoms with default values — wiping all state.\n\n**Why it happens:** During `renderHobView`, `startTracking()` is active. `(deref my-atom)` inside build functions calls `recordAtomAccess(atom)`, registering the atom as a dependency. Later, `(reset! my-atom ...)` fires `_atomMutationCallback` → rendering system finds dependents → schedules re-render → entire Hob code re-evaluated with fresh atoms.\n\n**The fix (platform-level):** Atoms are tagged with `_ownerCtx` (the tracking context ID active when they were created). `recordAtomAccess` skips self-owned atoms — atoms created by a view's own evaluation don't become dependencies of that view. This means `reset!` on view-local atoms drives local updates via `dom-set-content!` without triggering re-renders.\n\n**Symptoms if broken:** Click handlers produce a brief DOM flash then revert. State appears to reset after every interaction. Console may show `[Reactivity] Cycle detected`.\n\n## See Also\n\n- [Silent Update Pattern](item://c0c0c0c0-0010-0000-0000-000000000000) — Context for many of these gotchas\n- [Stale Closure Prevention](item://c0c0c0c0-0011-0000-0000-000000000000) — The most common source of bugs\n- [Spatial Windowing](item://ef793c27-2d4b-4c99-b05a-2769db5bc5a9) — Where most of these were discovered",
    "tags": [
      "af24a7ad-38c7-4660-b8ed-d67cf2062263"
    ]
  },
  "attachments": []
}