{
  "id": "c0c0c0c0-0062-0000-0000-000000000000",
  "name": "Rendering Architecture Review: Reactivity and the Spatial Canvas Question",
  "type": "871ae771-b9b1-4f40-8c7f-d9038bfb69c3",
  "created": 1739894400000,
  "modified": 1770467705060,
  "attachments": [],
  "content": {
    "tags": [
      "bff75873-edb8-412e-81c9-c744e68fe29f",
      "c0c0c0c0-0040-0000-0000-000000000000"
    ],
    "description": "# Rendering Architecture Review\n\n## Motivation\n\nWith the nested rendering fix complete (`c0c0c0c0-0061`), spatial-canvas-view conversion to Hob is technically unblocked. Before committing to that, this review examines whether spatial-canvas is the right next step, or whether foundational work in the rendering stack would be more valuable first.\n\nThe end goal is a simple and robust reactive rendering approach for Hob views. JS view compatibility can be dropped if it simplifies the design.\n\n## How the Rendering Stack Works Today\n\nThree layers combine to implement rendering:\n\n### Layer 1: Navigation (viewport-manager)\n\n`viewport-manager` (f1111111-0002) is a stateless library (~269 lines) that determines **what** to render. URL is authoritative for the root item ID. The viewport item (`88888888-...`) stores the view config (which view to use, layout state). `navigate()` updates URL + viewport item + triggers re-render via the rendering system.\n\n### Layer 2: Orchestration (kernel:rendering)\n\n`kernel:rendering` (33333333-5555) is the largest component (~1077 lines). It determines **how** to render:\n\n- **View resolution**: preference hierarchy (explicit viewId > item.preferredView > type.preferredView > type chain > default-view)\n- **Instance registry** (`RenderInstanceRegistry`): tracks every rendered DOM node by itemId, viewId, parentId, trackingId\n- **Dual render paths**: JS views call `viewModule.render()` then replaceChild; Hob views call `evaluate()` then `hiccupToDOM()` then morphdom\n- **Reactive subscriptions**: ITEM_UPDATED / ITEM_DELETED events + atom mutations > DependencyTracker lookup > microtask-batched re-renders\n- **Cleanup management**: `api._hobCleanups` > `__hobsonCleanup` on DOM > `cleanupDOMTree()` walks subtrees\n- **DependencyTracker**: defined in the Hob interpreter (40b00001) but initialized and driven by the rendering system\n\n### Layer 3: Views\n\nEach view is a function: `(item, api) -> DOM`.\n\n- **spatial-canvas-view** (ef793c27, ~617 lines JS + 5 libraries ~500 lines): manages window chrome (drag, resize, z-index, minimize, anchor/dock), delegates content to `api.renderItem()` for each child.\n- **Simple Hob views**: evaluate Hob code, return hiccup, converted to DOM via `hiccupToDOM()`.\n\n### Typical Render Flow\n\n1. User navigates > `viewport-manager.navigate()`\n2. > `rendering.rerenderItem(rootId)`\n3. > `renderItem()` resolves view (e.g., spatial-canvas-view)\n4. > View runs, calling `api.renderItem(childId)` for each window's content\n5. > Each child gets its own render instance + tracking context (the nested rendering fix)\n6. > Later: item changes > ITEM_UPDATED > dep tracker finds dependents > schedule re-render\n7. > For Hob views: full re-eval + morphdom patch; for JS views: full re-eval + replaceChild\n\n### View Inventory\n\n- **21 JS views** remain (13 are 3-4 line wrappers delegating to generic-view)\n- **24 Hob views** exist\n- **4 substantial JS views**: type-workshop-view (751 lines), spatial-canvas-view (618 lines + 5 libs), app-page-view (378 lines), test-suite-view (296 lines)\n\n## Weaknesses and Overcomplexity\n\n### 1. Dual rendering paths (JS vs Hob) — multiplicative complexity\n\nEvery feature needs two implementations:\n\n| Concern | JS path | Hob path |\n|---------|---------|----------|\n| Render | `viewModule.render()` > DOM | `evaluate()` > hiccup > `hiccupToDOM()` |\n| Re-render | `replaceChild` | morphdom |\n| Cleanup | Self-managed by view | `api._hobCleanups` > `__hobsonCleanup` |\n| Event handlers | Normal addEventListener | `__hobEvents` dict + morphdom transfer |\n| Dep tracking | Basic (records item access) | Full (items + atoms) |\n\nThe `__hobView` flag on DOM nodes is a runtime type tag that selects between these paths. `rerenderItem()` is ~200 lines of branching because of this duality.\n\n### 2. Whole-view reactivity is too coarse for complex views\n\nThe reactive unit is the entire view. Any dependency change triggers full re-evaluation of the entire Hob program, construction of a complete new DOM tree, and morphdom diffing against the old tree.\n\nFor a 10-element note view, this is fine. For spatial-canvas with 20 windows, this means re-evaluating all 20 windows' chrome code when one window moves, creating ~200 new DOM elements, and morphdom diffing all of them — only to discover that one element's `style.left` changed.\n\nThe current JS spatial-canvas avoids this with **direct DOM mutation**: drag handlers update CSS directly, then do a \"silent update\" (persist to DB without re-rendering). Converting to Hob without addressing this means either accepting the performance hit or reimplementing the same workaround — which undermines the reactive model.\n\n### 3. No independent reactive scope within a view\n\n`render-item` creates independent reactive scopes, but only for items. There's no way to create an independent reactive scope for a subpart of a view that isn't backed by a separate item.\n\nSpatial-canvas needs each window chrome to be independently reactive. Currently impossible without making each window chrome its own item (conceptually wrong) or using direct DOM mutation (breaks the reactive model).\n\nThe DependencyTracker already supports nested contexts via the `_prev` chain. The mechanism exists; it's just only accessible through `render-item`.\n\n### 4. DependencyTracker is architecturally misplaced\n\nDefined in the Hob interpreter (40b00001) but used by the rendering system for both JS and Hob views. The rendering system lazy-loads it from the Hob module. This means the rendering system needs the Hob language loaded to do any dependency tracking.\n\n### 5. Cleanup management is over-layered\n\nFive interconnected mechanisms: `api._hobCleanups` array, `__hobsonCleanup` function, `data-hobson-cleanup` attribute, `cleanupDOMTree()` subtree walker, and morphdom's `onBeforeElUpdated` event handler transfer. All exist because cleanup must survive morphdom patching.\n\n### 6. The parent re-render problem\n\nThis is the deepest issue. The rendering system hardcodes `this._depTracker.recordAccess(item.id)` in `renderHobView`, so every view depends on its own item. For spatial-canvas, the parent view depends on the parent item. When any attachment's metadata changes (z-index, position), the parent item changes, and the parent view re-renders.\n\nEven with independent child scopes (components), the parent re-renders. If child scopes are eagerly evaluated (as normal function calls would be), the parent re-render creates and then discards 20 child instances — all wasted work.\n\nThis means any fine-grained reactivity approach must address how the parent avoids doing wasted work when re-rendering.\n\n## Design Alternatives\n\n### Alternative 1: Lazy Components (React Model)\n\nAdd `defcomponent` to Hob. Parent produces lightweight descriptors (not evaluated DOM). Rendering system reconciles by key: existing components are skipped, new ones are mounted, removed ones are cleaned up.\n\n```\n;; Calling (window-chrome \"abc\" \"xyz\") returns a descriptor, not DOM\n[:__component {:key \"abc\" :impl window-chrome-impl :args [\"abc\" \"xyz\"]}]\n```\n\n`hiccupToDOM` recognizes component descriptors: if a matching instance exists (by key), returns the existing DOM node; otherwise evaluates the component.\n\n**Tradeoffs:**\n- (+) General-purpose, proven model, handles all cases\n- (+) Parent re-render is cheap (produces descriptors, no child evaluation)\n- (-) Significant machinery (reconciliation, keying, lifecycle management)\n- (-) Changes Hob evaluation semantics — a component call doesn't execute its body (surprising in a Lisp)\n- (-) Requires key-based identity for components in loops\n\n### Alternative 2: Data Granularity (Placement Items)\n\nNo new primitives. Restructure data so each independently-changing thing is its own item.\n\n```\nCanvas item:    { attachments: [\"placement-1\", \"placement-2\", ...] }\nPlacement item: { content: { target: \"child-item-id\", x: 100, y: 200, z: 5 } }\n```\n\nCanvas view renders each placement via `render-item` (existing independent reactivity). Window-chrome view reads position from placement item. Dragging updates only the placement item — only that window re-renders.\n\n**Tradeoffs:**\n- (+) Zero new primitives, works with existing `render-item` reactivity\n- (+) Parent only re-renders when placements are added/removed (canvas item only has IDs)\n- (-) Proliferates items (one placement per window per canvas)\n- (-) Position is view-state, not data — storing it as items blurs that boundary\n- (-) Not general-purpose; each complex view needs its own data restructuring\n- (-) May be worth doing anyway as a data modeling question independent of rendering\n\n### Alternative 3: Signals (Solid Model)\n\nFundamentally different rendering model. The view function runs **once** to set up a reactive graph. DOM nodes update directly when signal dependencies change. No re-evaluation, no morphdom.\n\n```\n(defn window-chrome [parent-id child-id]\n  (let [att (derive (fn [] (find-attachment (get-item parent-id) child-id)))]\n    ;; style.left updates directly when att changes\n    ;; No view re-render, no hiccup, no morphdom\n    [:div.window {:style {:left (str (:x (deref att)) \"px\")}}\n      (render-item child-id)]))\n```\n\n**Tradeoffs:**\n- (+) Maximum efficiency — surgical DOM updates, zero wasted work\n- (+) Eliminates morphdom entirely (and all its complexity: event handler transfer, input preservation, nested instance skipping)\n- (+) Arguably most Lisp-natural — expressions describe reactive relationships, not render snapshots\n- (-) Biggest departure from current model — complete paradigm shift\n- (-) Hiccup becomes a reactive template, not a pure data structure\n- (-) Can't mix with current \"eval > hiccup > morphdom\" approach without two rendering models coexisting\n- (-) Harder to debug — the view function ran once, possibly long ago\n- (-) Every piece of reactive data needs to be wrapped in a signal/derive\n\n### Alternative 4: Derived Scopes (render-view)\n\nGeneralize `render-item` to not require an item. A `render-view` creates an independent reactive scope parameterized by a selector function:\n\n```\n(defn spatial-canvas [item]\n  [:div.canvas\n    (for [child-id (map :id (:attachments item))]\n      (render-view\n        ;; Selector: re-runs when deps change, scope only re-renders when result changes\n        (fn [] (find-attachment (get-item (:id item)) child-id))\n        ;; View: receives selector result\n        (fn [att-data]\n          [:div.window {:style (pos-style att-data)}\n            (render-item child-id)])))])\n```\n\nOn parent item change: selectors re-run (cheap array lookup). Only changed scopes re-render.\n\n**Tradeoffs:**\n- (+) Explicit selector makes reactivity boundaries visible\n- (+) More composable than components (no lifecycle)\n- (+) Separates \"what data do I need\" from \"how do I render it\"\n- (-) Still needs lazy evaluation (same reconciliation machinery as Alt 1)\n- (-) Two functions per scope (selector + view) — more boilerplate\n- (-) Converges with Alt 1 in implementation; different mainly in ergonomics\n\n### Alternative 5: Selector-Filtered Reactivity (Diff-Aware Scheduling)\n\nITEM_UPDATED events already contain both the previous and current item data. Currently the rendering system ignores this — it just extracts the item ID and schedules re-renders for all dependents. This alternative uses that diff information to filter at the scheduling stage: scopes register a **selector** function when they record a dependency, and the rendering system only schedules a re-render when the selector returns a different value for old vs new.\n\n```\n;; Parent view registers: \"I only care about which children exist\"\n(watch-item! (:id item)\n  :select (fn [item] (map :id (:attachments item))))\n\n;; Window-chrome scope registers: \"I only care about my specific attachment entry\"\n(watch-item! parent-id\n  :select (fn [item] (find-attachment item child-id)))\n```\n\nWhen a z-index changes on one window:\n1. ITEM_UPDATED fires with previous + current parent item\n2. Parent view's selector: `(map :id old-attachments)` vs `(map :id new-attachments)` — same list — **skip**\n3. 20 window-chrome selectors: 19 return identical attachment entries — **skip**. 1 returns different entry — **schedule re-render**\n4. Total: 1 re-render of 1 window-chrome scope. Zero wasted work.\n\n**This dissolves the parent re-render problem without lazy evaluation.** The parent never re-renders for position/z-index/minimize changes because its selector filters them out. Since the parent doesn't re-render, it doesn't eagerly evaluate child scopes, so the lazy evaluation problem from Alt 1 simply doesn't arise. For structural changes (add/remove window — rare), the parent's selector detects a difference in the child ID list and re-renders normally.\n\nImplementation is a contained change to existing infrastructure:\n\n```javascript\n// DependencyTracker gains a selector registry\nrecordAccess(itemId, selector = null) {\n    // ... existing tracking code ...\n    if (selector) this.selectors.set(ctxId + ':' + itemId, selector);\n}\n\n// Reactive subscription checks selectors before scheduling\nthis.kernel.events.on(EVENT_IDS.ITEM_UPDATED, (event) => {\n    const { id, previous, current } = event.content;\n    const dependents = this._depTracker.getDependents(id);\n    for (const ctxId of dependents) {\n        const selector = this._depTracker.selectors.get(ctxId + ':' + id);\n        if (selector) {\n            if (deepEqual(selector(previous), selector(current))) continue;\n        }\n        this._pendingReRenders.add(ctxId);\n    }\n});\n```\n\n**Still requires independent scopes.** Selectors determine *when* a scope re-renders, but you still need a mechanism to define *what* constitutes a scope. The key insight is that the scoping mechanism can be much simpler than in Alt 1 — no lazy evaluation, no reconciliation, no keying. Simple eager components or `render-view` calls suffice, because selectors prevent the parent from re-rendering in the common case.\n\n**Tradeoffs:**\n- (+) Small, contained infrastructure change — selector registry + scheduling filter\n- (+) Dissolves the parent re-render problem for the common case (data changes) without lazy evaluation\n- (+) Backward compatible — scopes without selectors re-render on any change (current behavior)\n- (+) Works with any scoping mechanism (components, render-view, even plain render-item)\n- (+) Selectors are explicit and inspectable — easy to debug reactivity\n- (+) Uses information (the diff) that already exists but is currently discarded\n- (-) Still needs some form of independent scope for window chromes (not a complete solution alone)\n- (-) Selector functions run on every ITEM_UPDATED for every dependent — N selectors per item change (though each is cheap)\n- (-) Requires a `deepEqual` comparison (or user-provided equality check)\n- (-) Structural changes (add/remove) still cause full parent re-render (acceptable if rare)\n\n### Alternative 6: Do Nothing — Current Primitives Pragmatically\n\nConvert spatial-canvas to Hob as-is. Use atoms for transient state during drag. Use `invoke` for direct DOM mutation at 60fps. Let full morphdom happen on structural changes. Accept the cost.\n\n**Tradeoffs:**\n- (+) No new features to design/build/maintain\n- (+) Ship quickly, measure actual performance\n- (+) 20-window morphdom on ~200 elements might actually be fast enough\n- (-) Escape hatches (`invoke`, direct DOM mutation) make Hob code look like JS with extra syntax\n- (-) Sets precedent of working around the reactive model\n- (-) If it IS too slow, we've built on a foundation that needs replacing\n\n## Cross-Cutting Observations\n\n### Drag is always imperative\n\nRegardless of architecture, continuous 60fps drag requires direct DOM mutation. You can't round-trip through DB + re-eval at 60 times per second. The question is only what happens on mouseup (persist final position) — and whether that triggers a cheap targeted update or an expensive full re-render.\n\n### Discrete interactions (click to front, minimize) are the real test\n\nThese are the interactions where reactivity should \"just work\" without escape hatches. If clicking a window to bring it to front requires a silent update or direct DOM mutation, the reactive model has failed.\n\nWith components/derived scopes: update parent item > parent doesn't re-eval (lazy) > only the clicked window's scope re-renders (cheap). Reactivity works naturally.\n\nWithout components: update parent item > full canvas re-eval > full morphdom. Works but wasteful. Silent update avoids this but breaks the reactive model.\n\n### The parent re-render problem is fundamental\n\nEvery approach except Alt 2 (data restructuring), Alt 3 (signals), and Alt 5 (selectors) must contend with the parent re-rendering when any attachment metadata changes. Alt 1 solves this with lazy evaluation. Alt 5 solves it by filtering at the scheduling stage — the parent simply never re-renders for data-only changes. Alt 6 accepts it. This problem shapes the design more than any other factor.\n\n### Item-level dependency tracking is the root cause — but selectors may be enough\n\nThe current system tracks dependencies at item granularity: \"this context depends on item X.\" When item X changes in any way, all dependents re-render. Full path-level tracking (\"this context depends on item X's attachments[3].z\") would solve this but is a significant infrastructure change requiring proxy objects or similar.\n\nHowever, selector-filtered scheduling (Alt 5) achieves a similar practical result with much less machinery. Rather than tracking which paths are accessed, each scope declares what slice of the item it cares about. The rendering system compares that slice between old and new item on every update. This is coarser than true path-level tracking but sufficient for the known use cases, and the implementation is straightforward.\n\n## Assessment\n\n**Alt 5 (selectors)** is the most promising near-term approach. It's a small, contained change to existing infrastructure that dissolves the parent re-render problem for the common case. It needs to be paired with a scoping mechanism (independent reactive scopes for window chromes), but that mechanism can be simple — no lazy evaluation, no reconciliation. Selectors handle the hard part (when to re-render), leaving the scoping mechanism to handle the easy part (what to re-render).\n\n**Alt 3 (signals)** is the best long-term architecture but the biggest rewrite. It eliminates morphdom entirely, removing a huge source of complexity. Worth considering whether selectors are a stepping stone toward signals or an alternative path.\n\n**Alt 6 (do nothing)** is worth trying first as a measurement baseline. We don't actually know that 20-window morphdom is too slow.\n\n**Alts 1 and 4** converge on the same machinery (lazy descriptors + reconciliation) with different ergonomics. Selectors (Alt 5) may make these unnecessary — if the parent doesn't re-render in the common case, lazy evaluation is not needed.\n\n**Alt 2** (placement items) is interesting as a data-modeling question independent of rendering architecture.\n\n## Open Questions\n\n1. How slow is 20-window morphdom in practice? Should we measure before designing?\n2. If we add fine-grained reactivity, should it be the only model (replace whole-view reactivity) or coexist with it?\n3. Could signals and hiccup coexist — e.g., hiccup for static structure, signals for dynamic parts?\n4. Is the dual JS/Hob rendering path worth maintaining, or should we commit to Hob-only and take the conversion hit?\n5. Should DependencyTracker move regardless of which alternative we choose?\n6. For selectors (Alt 5): what equality semantics? Deep structural equality is correct but potentially expensive. Could use a user-provided comparator, or hash-based comparison, or reference equality with immutable data.\n7. For selectors + scopes: what's the simplest scoping mechanism that works? Could `render-view` be just a thin wrapper around the existing `render-item` machinery, parameterized by a selector + render function instead of an item ID?\n8. Are selectors a stepping stone toward signals, or an alternative path? Could you start with selectors + morphdom and migrate to signals later without breaking views?\n\n## Epilogue (Feb 2026)\n\n**Alternative 5 (Selector-Filtered Reactivity) was implemented and validated end-to-end.**\n\n### What was built\n\n1. **Selector-filtered reactivity** (commit `0b54c9e`): `get-item` accepts `:select fn` to declare what slice of data the view cares about. The rendering system runs selectors against old and new item data on every `ITEM_UPDATED` event, skipping re-renders when `deepEquals` shows no change.\n\n2. **Selector pins** (same commit): Once a view registers a selector for an item, later bare `get-item` calls on the same item don't widen the selector. The first selector is treated as the intentional reactivity contract.\n\n3. **Bare dep fallback** (same commit): `renderHobView` registers an implicit bare dep on `item.id` only AFTER evaluation, and only if the view didn't register its own dep. This preserves backward compatibility for simple views while letting spatial-canvas use a structural selector on its own item.\n\n4. **Spatial-canvas Hob conversion** (commit `30f478e`): ~490 lines of Hob replacing ~618 lines of JS. Uses a structural selector that watches only `[id, minimized, maximized, anchor]` per child — drag, resize, z-index changes produce SKIP. Libraries kept as JS.\n\n5. **Morphdom handler retention** (commit `30f478e`): An issue not anticipated in this review — morphdom's `onBeforeElUpdated` was transferring event handlers from the new (discarded) DOM tree to the old (kept) tree. New handlers captured stale refs to the discarded tree, causing close buttons and other actions to silently fail. Fixed by keeping old handlers, which have correct refs to kept elements.\n\n### Answers to open questions\n\n1. **How slow is 20-window morphdom?** — Not an issue in practice, because the selector filters mean the spatial-canvas almost never re-renders for cosmetic changes. When it does re-render (structural changes), performance is acceptable.\n\n2. **Should fine-grained reactivity replace whole-view?** — Selectors coexist naturally. Views without selectors get the existing behavior (re-render on any item change). The selector is opt-in per `get-item` call.\n\n3. **Signals and hiccup coexistence?** — Not explored yet. Selectors + morphdom are working well enough that signals aren't a near-term priority.\n\n4. **Dual JS/Hob path?** — Still maintained but converging. 19 of ~45 views are now Hob. The morphdom handler retention fix simplified both paths.\n\n5. **Should DependencyTracker move?** — Not yet, but less urgent. The selector registry lives in DependencyTracker (still in the interpreter), accessed by the rendering system. Works fine architecturally.\n\n6. **Equality semantics?** — Deep structural equality via `deepEquals`. In practice, selectors return small arrays of primitives (IDs + booleans), so comparison is fast.\n\n7. **Simplest scoping mechanism?** — Turns out independent scopes for window chromes weren't needed. The structural selector on the parent is sufficient: cosmetic changes → SKIP, structural changes → full re-render (acceptable since they're rare). Each child's content is already an independent scope via `render-item`.\n\n8. **Selectors as stepping stone to signals?** — Selectors work well enough as a standalone solution. They don't preclude signals later but also don't require them. The pragmatic answer is that selectors dissolved the parent re-render problem completely for the spatial-canvas case.\n\n### Key insight validated\n\nThe review's central claim was correct: **the parent re-render problem is the fundamental challenge, and selectors dissolve it without lazy evaluation**. The spatial-canvas never re-renders for position/z-index changes because its selector filters them out. Since the parent doesn't re-render, no child scopes are wastefully created and discarded. Alternatives 1 and 4 (lazy components, derived scopes) turned out to be unnecessary.\n\n## Related Items\n\n- Design Direction: `c0c0c0c0-0058` (Hob Language Design Direction)\n- Nested Rendering Fix: `c0c0c0c0-0061` (Investigation: render-item in for loops)\n- Hob View Conversion Tracker: `149546fb-4cf0-4a28-a79b-7cf313286bdf`\n- kernel:rendering: `33333333-5555-0000-0000-000000000000`\n- spatial-canvas-view: `ef793c27-2d4b-4c99-b05a-2769db5bc5a9`\n- viewport-manager: `f1111111-0002-0000-0000-000000000000`\n- Hob interpreter: `40b00001-0000-4000-8000-000000000000`"
  }
}
