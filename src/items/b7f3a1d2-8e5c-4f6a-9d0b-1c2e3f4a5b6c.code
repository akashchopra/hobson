(let [body (or (get-in item [:content :description]) "")]
  [:div {:style "line-height: 1.5;"}
    (cond
      (and (sequential? body) (= (first body) ":doc"))
        (for [node (rest body)] node)
      (and (string? body) (seq body))
        (render-markdown body)
      :else "")])